---
import PageLayout from '~/layouts/PageLayout.astro';
import Breadcrumbs from '~/components/directory/Breadcrumbs.astro';
import ListingDetail from '~/components/directory/ListingDetail.astro';
import ContactForm from '~/components/directory/ContactForm.astro';
import ReviewForm from '~/components/directory/ReviewForm.astro';
import AdSlot from '~/components/directory/AdSlot.astro';
import RelatedContent from '~/components/directory/RelatedContent.astro';
import NewsletterSignup from '~/components/directory/NewsletterSignup.astro';
import config from '~/config';
import citiesData from '~/data/cities.json';
import { getAllListings } from '~/lib/listings';

export async function getStaticPaths() {
  const listings = getAllListings();
  return listings.map(listing => ({
    params: { slug: listing.slug },
    props: { listing },
  }));
}

const { listing } = Astro.props;
const citySlug = listing.city.toLowerCase().replace(/\s+/g, '-');
const cityInfo = citiesData.find(c => c.slug === citySlug || c.city === listing.city);
const cityLabel = `${cityInfo?.city || listing.city}, ${listing.state}`;
const suffix = ` | ${cityLabel} | ${config.brandName}`;
const maxNameLen = 60 - suffix.length;
const truncatedName = listing.name.length > maxNameLen ? listing.name.slice(0, maxNameLen - 1).trim() + '…' : listing.name;
const title = `${truncatedName}${suffix}`;
const description = listing.description
  ? `${listing.name} — deposition videographer in ${cityInfo?.city || listing.city}, ${listing.state}. ${listing.description.slice(0, 120)}${listing.description.length > 120 ? '...' : ''}`
  : `${listing.name} is a deposition videographer in ${cityInfo?.city || listing.city}, ${listing.state}.${listing.certifications?.length ? ` Certifications: ${listing.certifications.join(', ')}.` : ''}${listing.rating ? ` Rated ${listing.rating}/5.` : ''} Request a free quote on ${config.brandName}.`;

const schema = {
  '@context': 'https://schema.org',
  '@type': 'LocalBusiness',
  name: listing.name,
  description: listing.description || `${listing.name} is a deposition videographer serving ${cityInfo?.city || listing.city}, ${listing.state}.${listing.services?.length ? ' Services: ' + listing.services.join(', ') + '.' : ''}`,
  address: {
    '@type': 'PostalAddress',
    streetAddress: listing.address,
    addressLocality: cityInfo?.city,
    addressRegion: listing.state,
  },
  telephone: listing.phone,
  url: listing.website,
  ...(listing.website ? { sameAs: [listing.website] } : {}),
  geo: {
    '@type': 'GeoCoordinates',
    latitude: listing.lat,
    longitude: listing.lng,
  },
  areaServed: {
    '@type': 'City',
    name: cityInfo?.city || listing.city,
  },
  ...(config.jobValue ? { priceRange: config.jobValue } : {}),
  ...(listing.certifications?.length ? {
    hasOfferCatalog: {
      '@type': 'OfferCatalog',
      name: 'Services',
      itemListElement: listing.services?.map(s => ({
        '@type': 'Offer',
        itemOffered: { '@type': 'Service', name: s },
      })) || [],
    },
  } : {}),
  ...(listing.certifications?.length ? {
    hasCredential: listing.certifications.map(c => ({
      '@type': 'EducationalOccupationalCredential',
      credentialCategory: 'Professional Certification',
      name: c,
    })),
  } : {}),
  ...(listing.rating > 0 && listing.review_count > 0 ? {
    aggregateRating: {
      '@type': 'AggregateRating',
      ratingValue: listing.rating,
      reviewCount: listing.review_count,
      bestRating: 5,
      worstRating: 1,
    },
  } : {}),
};

const ogImage = `/og/listing/${listing.slug}.png`;
const metadata = {
  title,
  ignoreTitleTemplate: true,
  description,
  openGraph: { images: [{ url: ogImage, width: 1200, height: 630 }] },
};
---
<PageLayout metadata={metadata}>
  <Fragment slot="announcement"><span></span></Fragment>
  <div class="bg-surface-page min-h-screen">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 py-6 sm:py-8" data-pagefind-body>
      <Breadcrumbs crumbs={[
        { label: cityInfo?.stateName || listing.state, href: `/states/${cityInfo?.stateSlug || 'all'}/` },
        { label: cityInfo?.city || listing.city, href: `/locations/${cityInfo?.stateSlug || 'all'}/${cityInfo?.slug || citySlug}/` },
        { label: listing.name },
      ]} />

      <ListingDetail listing={listing} />

      <div class="mt-4">
        <ContactForm listingName={listing.name} listingEmail={listing.email} listingSlug={listing.slug} city={cityInfo?.city || listing.city} />
      </div>

      <div class="mt-4">
        <ReviewForm listingSlug={listing.slug} listingName={listing.name} />
      </div>

      <div class="mt-4">
        <AdSlot placement="listing-bottom" city={cityInfo?.city} state={listing.state} />
      </div>

      <RelatedContent city={cityInfo?.city || listing.city} state={listing.state} currentSlug={listing.slug} />

      <div class="mt-6">
        <NewsletterSignup variant="compact" city={cityInfo?.city || listing.city} />
      </div>
    </div>
  </div>
  <script type="application/ld+json" set:html={JSON.stringify(schema)} />
</PageLayout>
